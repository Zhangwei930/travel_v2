# 周密出游 前端环境变量示例
# 复制成 .env.local（gitignore）按需覆盖

# 后端 API 根地址（必填，除了 H5 dev 模式可以留空走 vite proxy）
VITE_API_BASE=https://tr.magies.top/zhoumi-api

# 仅 H5 dev 模式使用 — 当 VITE_API_BASE 留空时，vite proxy 把 /api 转发到这里
VITE_DEV_PROXY=https://tr.magies.top/zhoumi-api
